{
  "name": "github-actions-demo",
  "version": "1.0.0",
  "description": "Projet d'exemple pour GitHub Actions",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 0",
    "lint": "eslint ."
  },
  "devDependencies": {
    "eslint": "^8.0.0"
  }
}
